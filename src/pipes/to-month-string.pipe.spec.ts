import { ToMonthStringPipe } from './to-month-string.pipe';

describe('ToMonthStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ToMonthStringPipe();
    expect(pipe).toBeTruthy();
  });
});
